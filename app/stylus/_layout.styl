*
  box-sizing border-box

img
  max-width 100%

a
  text-decoration none

html
  font-size $baseFontSize

html, body, .wrapper
  position relative
  -webkit-font-smoothing antialiased
  -moz-osx-font-smoothing grayscale
  -webkit-font-smoothing antialiased
  text-rendering optimizeLegibility !important

body
  position relative
  min-width 375px
  min-height 100vh
  margin 0
  padding 0
  font-family $baseFontFamily
  font-weight $baseFontWeight
  box-sizing border-box
  overflow-x hidden

  &.blocked, &.open-menu
    overflow hidden
    -ms-touch-action none
    touch-action none

  .header, .footer, .main
    opacity 0
    transition .5s

  &.pace-done
    .header, .footer, .main
      opacity 1

      .pace
        display none

    .decor__item
      transform scale(1)
      opacity 1

      @media $desktopMax
        transform scale(.7)

  &.open-menu
    .decor--mobile
      opacity 1
      z-index 10

.wrapper
  position relative
  overflow-x hidden
  min-height 100vh

  &--inside
    padding-top 176px

    .decor
      position absolute
      left 0
      top 0
      width 100%
      z-index 0

      &__item
        &--n6
          top 205px
        &--n2
          top 200px
          left 70vw
        &--n3
          top 40vh
          left calc(100vw - 91px)
        &--n4
          top 70vh
          left -52px

    @media $desktopMax
      padding-top 102px

    .header
      background-color #F3F8FF
      padding-bottom 71px

      @media $desktopMax
        padding-bottom 60px

      &:after
        content ''
        position absolute
        display block
        left 0
        bottom 0
        width 100%
        height 31px
        background url('../../../../static/img/wave3.png') no-repeat center

    .main
      padding 79px 0 149px

      @media $desktopMax
        padding-bottom 96px

      @media $tLandscapeMax
        padding-bottom 100px

      @media $mLandscapeMax
        padding-bottom 70px
